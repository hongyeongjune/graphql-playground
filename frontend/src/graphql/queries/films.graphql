query Films($filmId: Long, $limit: Long) {
    getFilms(filmId: $filmId, limit: $limit) {
        cursor
        data {
            id
            title
            subtitle
            genre
            runningTime
            releaseDate
            posterImg
            director {
                id
                name
            }
        }
    }
}